todo:
提供验证机制
分布式
以上两个功能归纳在新的分布式服务中
验证机制：
validator授权验证，intercept消息中的标识，
标识可以手动添加也可以预先配置好。
标识可以是guid也可以是密码+用户名，可以实现方法级标识
可以自定义标识validate借口，也可以系统提供

基础rpc不包括服务注册中心


消息应答模型：包含一个消息请求和消息服务
消息请求发送请求，消息服务获取后，可以在绑定的消息服务执行并返回响应。
